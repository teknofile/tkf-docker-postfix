---

# project information
project_name: postfix
project_url: "http://www.postfix.org"
project_logo: "http://www.postfix.org/mysza.gif"
project_blurb: "[{{ project_name|capitalize }}]({{ project_url }})  It is Wietse Venema's mail server that started life at IBM research as an alternative to the widely-used Sendmail program. Now at Google, Wietse continues to support Postfix."
project_tkf_github_repo_url: "https://github.com/teknofile/tkf-docker-{{ project_name }}"

# supported architectures
available_architectures:
  - { arch: "{{ arch_x86_64 }}", tag: "amd64-latest"}

# container parameters
param_container_name: "{{ project_name }}"
param_usage_include_vols: true
param_volumes:
  - { vol_path: "/config", vol_host_path: "</path/to/postfix/config>", desc: "Configure the `Targets` file here" }
param_usage_include_ports: true
param_ports:
  - { external_port: "25", internal_port: "25", port_desc: "Allows SMTP access to the internal mail server." }
  - { external_port: "465", internal_port: "465", port_desc: "Allows SSMTP access to the internal mail server." }
param_usage_include_env: true
param_env_vars:
  - { env_var: "TZ", env_value: "Europe/London", desc: "Specify a timezone to use EG Europe/London"}

# changelog
changelogs:
  - { date: "10.05.19:", desc: "This is the first release, it is mostly stable, but may contain minor defects. (thus a beta tag)" }

